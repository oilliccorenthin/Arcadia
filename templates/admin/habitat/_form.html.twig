{# _form.html.twig #}

{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    {{ form_row(form.images) }}
    
    <button type="submit" class="btn btn-primary">{{ button }}</button>
{{ form_end(form) }}